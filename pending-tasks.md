# Pending tasks — 2026-06-01

## 1) [DEFERRED — needs n8n surgery] Message Generator: OpenAI → Claude Haiku

The /api/campaigns/generate-messages route is a thin proxy: it only
forwards `{leadId, icpProfileId, companyBioId, templateId, sequence...}`
to the n8n webhook `https://n8n.srv949269.hstgr.cloud/webhook/
generate-campaign-messages-v3`. The actual LLM call lives inside the
n8n workflow `V8 Native Generator` (id `Y3gQXLpaWjpP37XP` per memory
`project_v8_architecture.md`).

Migrating to Haiku is therefore a **workflow change, not a code change**.
Steps the operator (Fran) needs to do — none of these can be automated
safely without touching production:

  1. Backup the live workflow JSON to `workflows/archive/v8-native-
     generator-pre-haiku-<date>.json`.
  2. In the n8n editor, open the workflow. Identify the AI Agent /
     LLM node currently configured for OpenAI (probably `gpt-4o-mini`
     or `gpt-4o`).
  3. Add Anthropic credentials to n8n (Settings → Credentials → New →
     Anthropic API). Use the same `ANTHROPIC_API_KEY` already wired into
     the CRM (.env.local) so accounting stays consolidated.
  4. Swap the model to `claude-haiku-4-5-20251001`. Output format stays
     JSON; Haiku's instruction-following is sufficient (proven by the
     /api/companies/hooks endpoint that shipped 2026-06-01).
  5. Run a ghost-test against a sandbox lead before activating. The
     wizard's `ChannelMessageConfig.tsx` triggers the call on every
     campaign approve — a bad output blocks new campaigns.
  6. Deploy. Watch the next 5 generations live for parity vs. OpenAI
     output before declaring done.

The CRM-side `app/api/campaigns/generate-messages/route.ts` requires
ZERO changes for this — it just relays IDs.

## 2) [DONE 2026-06-01] Lost-reason capture from History

Shipped in `b131059`. Zero-migration approach: stored as a lead_replies
row with `[LOST_REASON]` prefix; PATCH /api/leads/[id]/status accepts an
optional `reason` field. UI: `/leads/lost/[id]` shows the panel above
the stats row; if the reason is empty the card outlines red to draw the
eye, and Save persists + refreshes server side.

## 3) [DONE 2026-06-01] Company-detail Call Hooks panel

Shipped. AI-generated by Claude Haiku from the lead's enrichment fields
(description, tech stack, news, LinkedIn posts, mission, etc.). Endpoint:
POST /api/companies/hooks → 3–5 short, specific hooks. Component
`CompanyHooksPanel` renders at the top of the company overview tab with
a Generate / Refresh button; results cache in localStorage by leadId so
revisits feel instant and re-runs are explicit.

## 4) [DONE 2026-06-01] Phone picker (secondary number)

Not a code bug. The test lead (Francisco Test) only has `primary_phone`;
`primary_secondary_phone` was null. The picker hides when only one phone
exists. Verified with 5 dual-phone leads in DB (Alicia @ COFIDES, Rafael
@ Buenavista, Gary @ Rhodes, Stephen @ LJ Property, Mateo @ PAI) — the
chip renders correctly for those.
